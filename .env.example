GEMINI_API_KEY=your_gemini_api_key_here
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/your_db
PORT=3000

# Notes: Do NOT store real secrets in the repository. Use environment variables in CI or a secrets manager for production.